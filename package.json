{
  "name": "project-sora",
  "version": "1.0.0",
  "private": true,
  "description": "Project Sora is a browser-based game library and discovery dashboard.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "backup": "node server.js --backup",
    "validate-backup": "node server.js --validate-backup",
    "test": "node --test tests/*.test.js tests/*.test.mjs",
    "syntax-check": "find . -type f \\( -name '*.js' -o -name '*.mjs' \\) ! -path './node_modules/*' | sort | while read -r file; do node --check \"$file\" || exit 1; done",
    "verify": "npm run syntax-check && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.2"
  }
}
